# 线程的状态转换

## 线程有哪些状态？

线程的状态

1. New (新创建)
2. Runnable (可运行)
3. Blocked (被阻塞)
4. Waiting (等待)
5. Time-Waiting (计时等待)
6. Terminated (被终止)

线程在任意一个时间点只能处于一种状态。

其中 Blocked、Waiting、Time-Waiting 统称为阻塞状态。

### New 新创建

当 `new Thread ()` 一个线程但没有调用 `start ()` 方法时，线程处于 New 状态。

### Runnable 可运行

当调用 Thread 对象的 start 方法时，线程状态将从 New 转换为 Runnable。

Java 线程的 Runnable 状态对应操作系统中的 Ready 和 Running 状态。也就是说处于 Runnable 状态的 Java 线程此时可能正在执行也可能正准备执行。

### Blocked 被阻塞

只有线程进入 synchronized 代码块且没有拿到锁时，线程进入 Blocked 状态。

当线程获取到锁后，状态由 Blocked 转为 Runnable 状态。

### Waiting 等待

进入 Waiting 状态的几种情况：

1. 调用没有 timeout 参数的 `obj.wait ()` 方法
2. 调用没有 timeout 参数的 `thread.join ()` 方法
3. 调用 `LockSupport.park ()` 方法

### Time-Waiting 计时等待

## Object 对象中的基本方法

```java
public final void wait () throws InterruptedException

public final void wait (long timeout) throws InterruptedException

public final void wait (long timeout, int nanos) throws InterruptedException

public final void notify ();

public final void notifyAll ();
```

### wait ()

wait 方法分为带参数和不带参数两种。

在线程获取到锁之后，发现当前不满足线程执行的条件，可以调用锁对象的 wait 方法释放锁，并将该线程放到与锁关联的等待队列中。

此时线程从 `Runnable` 转换为 `Waiting` 状态。

带参数的 wait 方法与 不带参数的 wait 方法类似，在线程获取到锁之后，发现当前不满足线程执行的条件，可以调用锁对象的 wait 方法释放锁并指定一个 timeout 时间，并将该线程放到与锁关联的等待队列中，当线程在等待队列中等待的时间超过 timeout 设置的时间后会被自动从等待队列中移出。

### notify () 和 notifyAll ()

锁对象 notify 方法的作用是随机唤醒一个与当前锁关联的线程等待队列中的线程。(随机性取决于 JVM 的实现，JDK1.8 Hotspot 是顺序唤醒的)

notifyAll 方法则是唤醒当前锁所关联的线程等待队列中所有的线程。

所谓的唤醒就是让等待队列中的线程状态转换为 Runnable 活 Blocked 状态 (线程未获取到 synchronized 代码块锁)

notify 或 notifyAll 会立即释放锁吗？

```java
public void test () {
    Object objectLock = new Object ();
    synchronized (objectLock){
        objectLock.notifyAll ();
        while (true){
        }
    }
}
```

答案是否定的，例如上述代码，objectLock 锁在调用完 notifyAll 之后进入了一个死循环，这会导致 objectLock 锁所关联的线程等待队列中的所有队列永远不会运行，因为 objectLock 锁始终没有被释放。

```java
public static void main (String [] args) {
    Object objectLock = new Object ();
    new Thread (() -> {
        synchronized (objectLock) {
            System.out.println (Thread.currentThread ().getName () + "开始执行！");
            try {
                objectLock.wait ();
            } catch (InterruptedException e) {
                e.printStackTrace ();
            }
            System.out.println (Thread.currentThread ().getName () + "被唤醒！");
        }
    }, "t1").start ();

    new Thread (() -> {
        synchronized (objectLock) {
            objectLock.notify ();
            System.out.println (Thread.currentThread ().getName () + "发出通知！");
        }
    }, "t2").start ();
}
```

## 为什么 wait 必须在 synchronized 保护的同步代码块中使用

wait 方法的一般使用场景是在判断某种条件不满足且需要等待其他线程先执行后执行。

例如下方代码，会判断 list 是否为空，当 list 为空时线程进入等待状态。

可是条件判断和执行并不是原子性的，有可能条件判断完后线程执行终止了。

其他线程向 list 放入数据后唤醒 ThreadA，因为 ThreadA 已经完成条件判断，开始进入 wait 状态。

这样是不对的，因为此时的条件判断结果是过期的。

判断和执行不是原子性的，是线程不安全的。

而且 wait 方法会释放 monitor 锁，因此需要我们在调用 wait 方法时要获取到 monitor 锁。

```java
public class ThreadA extends Thread {

  List<String> list = new ArrayList<>();
  Object objectLock = new Object ();
  @Override
  public void run () {
    while (list.isEmpty ()) {
      objectLock.wait ();
      // 从 list 里面消费数据
    }
    //do others
  }
}

```

### 虚假唤醒

虚假唤醒即线程在既没有被 notify 和 notifyAll 也没有被中断或超时的情况下被唤醒。

## 为什么 wait、notify、notifyAll 被定义在 Object 类中，而 sleep 被定义在 Thread 类中

因为 Java 中每一个对象都有一把被称之为 Monitor 监视器的锁。

每个对象都可以上锁，这就要求在对象头中有一个保存锁信息的位置。

这个锁是对象级别的。 wait、notify、notifyAll 也都是锁级别的操作。

## wait 和 sleep 的区别

1. 无参 wait 执行后需要被唤醒。sleep 则会在休眠指定时间后自动唤醒。
2. wait 会释放锁，sleep 不会。
