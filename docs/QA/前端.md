# 前端问题及处理流程记录

## Avue Cascader 级联选择器报错

column 配置：

```js
{
  label: "所属分类",
  prop: "categoryId",
  type: "cascader",
  props: {
    label: "title",
    value: "id"
  },
  emitPath:false,
  rules: [{
    required: true,
    message: "请选择所属分类",
    trigger: "blur"
  }],
  dicUrl: "/api/doublemall-product/category/tree"
}
```

```js
error.js:20 TypeError: o.split is not a function
    at le (avue.min.js:9:74741)
    at VueComponent.handleDetail (avue.min.js:9:80562)
    at fn (avue.min.js:9:85239)
    at Object.normalized [as default] (vue.js:2244:43)
    at e.type.e.renderCell (index.js:1:243924)
    at index.js:1:216731
    at Array.map (<anonymous>)
    at VueComponent.rowRender (index.js:1:216052)
    at VueComponent.wrappedRowRender (index.js:1:218167)
    at index.js:1:211161
```

```js
var e = arguments.length > 0 && void 0 !== arguments [0] ? arguments [0] : {}
  , t = arguments.length > 1 && void 0 !== arguments [1] ? arguments [1] : {}
  , n = arguments.length > 2 && void 0 !== arguments [2] ? arguments [2] : {}
  , i = arguments.length > 3 && void 0 !== arguments [3] ? arguments [3] : []
  , o = e [t.prop]
  , r = t.type
  , s = t.separator;
```

从控制台的日志可以看到：变量 o 没有 split 这个函数

而 spirit 是 string 类型变量的函数，若 o 是其他类型，比如数值型 1，则 `1.split` 就会报错

通过对 `o = e [t.prop]` 打断点，可以看到

```js
var l = a.k.includes (t.type) && t.multiple, c = a.b.includes (t.type);
if (!l && !c ||
    Array.isArray (o) ||
    t.dataType || 
    (t.dataType = "string"), t.dataType &&
    (l || c ? (Array.isArray (o) || (o = o.split (s || a.h)), o.forEach ((function (e) {
                    e = Object (S.f)(e, t.dataType)
                }
                ))) : o = Object (S.f)(o, t.dataType)),
                "password" === r)
```

解决办法：

将 prop 修改为 cateId

原因：prop 为 categoryId 时就报错，具体原因未知
