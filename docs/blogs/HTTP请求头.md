# HTTP 请求头标签

## if-modified-since

主要作用：

节约网络流量

工作原理：

浏览器在缓存服务器文件的同时也会缓存文件的最后修改时间。

再次向服务器请求该文件时可以通过 `if-modified-since` 携带，服务器会将文件的最后修改时间与`if-modified-since` 携带的时间对比。

如果时间一致则返回状态码 304，浏览器会使用缓存总的文件。

如果时间不一致则正常返回状态码 200 和新文件。


扩展：

- ***etag***
